#  Body - Updates ===

# Practice Task
# Update your Task Manager — add a PATCH /tasks/{task_id} endpoint:

# Get stored task
# Convert to Task model
# Use exclude_unset=True to get only sent fields
# Use model_copy to merge
# Save and return

# Test — update only completed: true on a task and confirm title stays unchanged.

